{
  "name": "@hiai-docs/mcp-server",
  "version": "0.8.1",
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "bin": {
    "hiai-docs-mcp": "./src/index.ts"
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "test": "bun run --filter '@hiai-docs/sdk' build && bun test --path-ignore-patterns=\"*node_modules*\" src",
    "test:unit": "bun run test",
    "test:contract:scoped-live": "bun run --filter '@hiai-docs/sdk' build && ATTACHMENT_PRESIGN_EXPIRY_SECONDS=60 bun test tests/integration/scoped-public-surfaces.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hiai-docs/sdk": "workspace:*",
    "@modelcontextprotocol/server": "2.0.0",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@modelcontextprotocol/client": "2.0.0",
    "@types/bun": "^1.4.0",
    "typescript": "6.0.3"
  }
}
