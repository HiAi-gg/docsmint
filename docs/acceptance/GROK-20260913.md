# DocsMint OSS acceptance — 2026-09-13

Project: `docsmint-oss` at `/mnt/data/projects/docsmint-oss`.
Worked in place. No commit, push, tag, release, deploy, copies, or sibling edits.

Independent review return addressed: nDCG fixture regenerated from the canonical evaluator, TS2352 removed by validating fixture labels, lint format fixed, source-string assertions replaced with authorization/SDK/CLI behavior.

## Tasks

### DOCSMINT-OSS-T01 — 0.8.3 editing/access regression coverage

Status: **review**.

Production: `canManageWorkspaceTags()` used by tag collection routes; `buildApiAccessValues()` extracted to `backend/src/lib/category-api-access.ts` and used by category PATCH/create.

Behavior (not grep):

- Autosave retries stay bound to document A after switching to B (frontend 8 pass / 22 expect).
- Category PATCH merge keeps omitted permissions (`buildApiAccessValues` unit + HTTP name-only PATCH).
- Category-scoped write key receives 403 on `POST /api/tags` and creates no row; operator key creates a tag.
- Attachment DELETE with storage failure keeps the original `storageKey`/`sourceId` on the cleanup outbox.
- Attachment mutation policy: edit scope required; write-only keys cannot edit; other categories denied via `isAuthorizedCategory` + `effectiveDocumentCategory`.

The in-memory route harness does not evaluate SQL `effectiveDocumentCategoryCondition`, so attachment category denial is proven at the access-policy layer, not by mock SQL.

### DOCSMINT-OSS-T02 — Retrieval failure/evaluation matrix

Status: **review**.

Canonical evaluator: `backend/src/search/eval/offline-eval.ts`, used by `eval-retrieval.ts --mode=baseline`.

nDCG semantics: graded DCG with `gain / log2(rank+1)` (`dcg` in `eval-metrics.ts`), divided by ideal DCG of the label vector. Textbook check: `ndcgAt(["b","a"], {a:3,b:2}) === dcg([2,3]) / dcg([3,2])`.

The previous 0.814986 figure under-counted query `exact-title`: after ranking `doc-rrf` first, `doc-graph` (gain 1) remains in the list, so nDCG is 0.96394 for that query, mean **0.826458**. Per-query rows are recorded in `retrieval-failure-matrix.json`.

Failure cases still execute: last-ready generation, rerank ID mapping after empty-text filter, rerank fail-open identity, unauthorized graph neighbors dropped.

### DOCSMINT-OSS-T03 — REST/SDK/CLI/MCP packaging compatibility

Status: **review**.

- Frozen 0.5.0 package exports still match `package.public.json`; each published export path exists under in-place `packages/sdk/dist` (no pack/copy).
- `DocsClient` health, search, and createDoc hit `/api/health`, `/api/search`, `/api/documents`.
- CLI `listDocuments` + `search` hit `/api/documents` and `/api/search` with the configured bearer key.
- MCP `mcp-server.js` / `mcp-cli.js` artifacts exist; existing MCP protocol tests remain in `packages/mcp-server/src/server.test.ts`.
- Inventory has no `/billing`, `/api/chat`, or subscription routes.

Packed-tarball smoke was not run (owner forbids copies).

## Check commands

| Command | Exit | Count / notes |
| --- | --- | --- |
| `run-check.py bun --no-env-file test ...` 7 backend unit files | **0** | 37 pass / 0 fail / 467 expect |
| `run-check.py bun --no-env-file test tests/integration/routes.editing-access-regression.test.ts` | **0** | 3 pass / 0 fail / 12 expect |
| `run-check.py bun test ... document-autosave.test.ts` | **0** | 8 pass / 0 fail / 22 expect |
| `run-check.py bun test ... packages/cli/src/client.test.ts` | **0** | 2 pass / 0 fail / 5 expect |
| `run-check.py bun --cwd=/mnt/data/projects/docsmint-oss/backend --no-env-file run typecheck` | **0** | `tsc --noEmit` |
| `run-check.py bun --no-env-file run lint` | **0** | backend 268 files, frontend 241 files, no fixes |
| `run-check.py bun --cwd=/mnt/data/projects/docsmint-oss/backend --no-env-file run build` | **0** | bundled 1884 modules |
| Root `bun run typecheck` | **1** | `EACCES accessing temporary directory` for `bunx tsc` scripts. Environment blocker, not a source error. |

A `test:e2e` placeholder is not browser coverage. No live rerank, Docker, Postgres, or production verification.

## Remaining work

- Optional live `eval:retrieval --mode=rerank --live` if an operator supplies a provider key.
- Packed npm smoke needs coordinator-approved artifact-only staging; not run here.
- Root workspace typecheck/scripts blocked by `BUN_TMPDIR` permissions.
- In-memory attachment category SQL is not executed; policy tests cover the grant, not AGE/SQL.
- Coordinator acceptance and GitHub update remain outside this session.
