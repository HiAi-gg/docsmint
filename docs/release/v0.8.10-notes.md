# DocsMint 0.8.10 — document-list contract correction

## Highlights

- Return folder and category names from the offset document-list API, matching
  the published SDK type and MCP `list_documents` output schema.
- Keep structured `list_documents` results valid for documents with and without
  folder/category placement.

## Compatibility and operations

- No database migration is required.
- Document visibility, permissions, pagination, and MCP tool behavior are
  unchanged; the response now includes the labels already promised by the SDK.

## Verification

- Document route integration regression covers populated folder/category names.
- Release CI verifies all 21 MCP tools through the assertion-bound public client.
